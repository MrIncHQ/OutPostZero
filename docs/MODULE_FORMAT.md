# Portable Module Format

The module system is not implemented in the portability milestone.

Future modules must include a signed manifest declaring their identity, version, supported platforms and architectures, sizes, owned and shared directories, dependencies, runtime command, health check, network policy, license, checksums, and package signature. Modules may write only through paths approved beneath the portable root and may not require host installation or elevation.
